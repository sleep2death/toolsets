mixin button(id, path, title)
  include icon
  button.h-10.flex.flex-col.justify-center.text-gray-600(id=id class="hover:text-blue-500")
    .w-32.flex.flex-row.justify-center
      +icon(path)
      .select-none= title
